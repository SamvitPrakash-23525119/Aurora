import { StyleSheet } from "react-native";
import { GENERAL_SECONDARY } from "../global/colors";

export const styles = StyleSheet.create({
    PAGE_CONTAINER: {
        backgroundColor: GENERAL_SECONDARY,
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
    },

});
